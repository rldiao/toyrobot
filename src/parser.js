'use strict'
// Parser for robot commands
const fs = require('fs');

const parser = {
    parse: function parse(inputFile) {
        // Parse input
        /* Your Code */
    }
}

module.exports = parser;