'use strict'
// Imports
const constants = require('./constants');
const Robot = require('./Robot');
const Table = require('./Table');
const parser = require('./parser');

function main() {
    const table = new Table(constants.tableDimensions.width, constants.tableDimensions.length);    

    /* What to do:
     * 1. Put the robot on the table
     * 2. Parse the input text files
     * 3. Let the robot do those commands from input
     * 4. ??
     * 5. Profit
     */

}

main();